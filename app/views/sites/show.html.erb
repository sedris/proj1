<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @site.name %>
</p>

<p>
  <b>Base url:</b>
  <%= @site.base_url %>
</p>

<b>All Visits</b><br>
<table>
  <tr>
    <th>URL</th>
    <th>Duration</th>
  </tr>

<% @allVisits.each do |visit| %>
  <tr>
    <td><%= visit.url %></td>
    <td><%= visit.duration %></td>
  </tr>
<% end %>
</table>

<br>
visits hash: <%= @visits%>
<br>
avg_durations hash<%= @avg_durations%>
<br><br>
<table>
  <tr>
    <th>URL</th>
    <th>Visits</th>
    <th>Avg Duration</th>
  </tr>

<% @visits.each do |key, value| %>
  <tr>
    <td><%= key %></td>
    <td><%= value %></td>
    <td><%= @avg_durations[key] %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Edit', edit_site_path(@site) %> |
<%= link_to 'Back', sites_path %>
